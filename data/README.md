# Data

`sample_transactions.csv` contains reproducible, anonymous synthetic data only. Regenerate it with:

```bash
python -m anomaly_detector --input data/sample_transactions.csv simulate-data
```

No real private transaction records are included in this repository.
